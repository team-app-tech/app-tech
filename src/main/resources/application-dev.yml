spring:
  datasource:
    url: jdbc:mysql://mysql:3306/pangta?serverTimezone=UTC&characterEncoding=UTF-8
    username: pangta
    password: pangta
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
#        show_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
